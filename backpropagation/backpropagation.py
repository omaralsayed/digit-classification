'''
Feed-forward multi-layer neural network with backpropagation including momentum.
Able to handle any number of hidden layers and hidden neurons (specify at run-time).
'''
import dataset

mnist = dataset.mnist()

